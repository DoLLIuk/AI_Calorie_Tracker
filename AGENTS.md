# Local Codex Rules

## Skill policy
- Do not auto-trigger the `universal-engineer` skill based on task similarity.
- Use the `universal-engineer` skill only when the user explicitly mentions `universal-engineer`, `$universal-engineer`, or clearly asks to use that exact skill by name.

